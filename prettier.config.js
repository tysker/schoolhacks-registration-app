const config = {
    plugins: ["prettier-plugin-tailwindcss"],
    tailwindConfig: "./tailwind.config.js",
    formatOnSave: true,
    formatOnPaste: true,
    singleQuote: true,
}

export default config;